acsVersion: "0.3.1-beta"
name: mcp-policy
defaultAction: allow
rules:
  - name: block-user-data
    condition: "tool.name == 'query_user_data'"
    action: deny
    description: "Strict AuthZ required for user data"
