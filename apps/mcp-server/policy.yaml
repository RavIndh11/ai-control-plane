apiVersion: governance.toolkit/v1
name: mcp-policy
default_action: allow
rules:
  - name: block-user-data
    condition: "action.type == 'query_user_data'"
    action: deny
    description: "Strict AuthZ required for user data"
